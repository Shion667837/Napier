<div class="right-wrapper" id="nav-bar">
  <nav id="right-sidebar">
    <div class="sidebar-item-fixed">
      <div class="sidebar-main-host">
      </div>
      <div class="nav_list">
        <h2 class="right-nav-top">おすすめユーザー</h2>
        <div class="sidebar-list">
          <div class="cp_box">
            <input id="cp01" type="checkbox">
            <label for="cp01"></label>
            <div class="cp_container">
              <ul class="users">
                <div class="users-index">
                  <% @recommenusers.each do |user| %>
                    <li>
                      <div class="index-user-icon">
                        <% if user.image_name? %>
                          <%= image_tag user.image_name.url, class: "host-icon" %>
                          <span class="host-name"><%= link_to user.name, user %></span>
                        <% else %>
                          <%= image_tag "/user_images/default.png", class: "host-icon" %>
                          <span class="host-name"><%= link_to user.name, user %></span>
                        <% end %>
                      </div>
                    </li>
                  <% end %>
                </div>
              </ul>
            </div>
          </div>
        </div>
        <div class="sidebar-list comming-soon">
          <p>Coming soon...</p>
        </div>
      </div>
    </div>
  </nav>
</div>
